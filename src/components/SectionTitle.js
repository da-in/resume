import React from 'react';

function SectionTitle({ children }) {
    return (
        <p className="handwriting" style={{ textAlign: 'center' }}>
            {children}
        </p>
    );
}

export default SectionTitle;
